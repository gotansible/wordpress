server {

	listen 8080;
	server_name localhost;

	root /var/www/phpinfo;

	index info.php index.php index.html index.htm;

	location / {
		try_files $uri $uri/ =404;
		{% for ip in wordpress_stats_ips %}
		allow {{ ip }};
		{% endfor %}
		deny all;
	}
	
	include hhvm.conf;
}
